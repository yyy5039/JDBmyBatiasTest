<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 为这个mapper指定一个唯一的namespace，namespace的值习惯上设置成包名+sql映射文件名，这样就能够保证namespace的值是唯一的
例如namespace="me.gacl.mapping.userMapper"就是me.gacl.mapping(包名)+userMapper(userMapper.xml文件去除后缀)
 -->
<mapper namespace="me.gacl.mapping.usermaper">
    <!-- 在select标签中编写查询的SQL语句， 设置select标签的id属性为getUser，id属性值必须是唯一的，不能够重复
    使用parameterType属性指明查询时使用的参数类型，resultType属性指明查询返回的结果集类型
    resultType="me.gacl.domain.User"就表示将查询结果封装成一个User类的对象返回
    User类就是users表所对应的实体类
    -->
    <!-- 
        根据id查询得到一个user对象
         <collection property="goods" column="id" javaType="java.util.ArrayList"    
         select="me.gacl.mapping.goodsmaper.getGoodsByOwner"
         ofType="java.util.Map"/>
     -->
     
    <resultMap id="userMap" type="me.gacl.domain.Myuser">
        <id property="id" column="id" javaType="java.lang.Integer"></id>
        <result property="name" column="name" javaType="java.lang.String"></result>
        <result property="age" column="age" javaType="java.lang.Integer"></result>
        <collection property="goodList" column="id" javaType="java.util.ArrayList" 
        ofType="me.gacl.domain.Goods"
        select="me.gacl.mapping.goodsmaper.getGoodsByOwner"/>

    </resultMap>

    
    <insert id="insertUser" parameterType="me.gacl.domain.Myuser">  
        INSERT INTO USERS (ID,NAME,AGE)
        VALUES   (#{id},
                  #{name},
                  #{age})
    </insert>
    
    <update id="updateUser" parameterType="me.gacl.domain.Myuser" >
      update users set name=#{name},age=#{age} where id=#{id}
    </update>
    
     <delete id="deleteUser" parameterType="int" >
      delete from users where id=#{id}
    </delete>

    <select id="selectUserById" parameterType="int" resultMap="userMap">
        select * from users where id=#{id}
    </select>
    
     <select id="getUserById" parameterType="int" resultMap="userMap">
        select * from users where id=#{id}
    </select>

    <select id="selectUserByIdAndAge" parameterType="java.util.Map"
        resultMap="userMap">
        select * from users where id=#{id} and age=#{age}
    </select>    
    
    
    <select id="selectAllUser" resultType="me.gacl.domain.Myuser">
        select * from users
    </select>    
    
    <update id="upUserIDBatch" parameterType="java.util.List">
    <foreach collection="list" item="item" index="index" open="" close="" separator=";">
        UPDATE USERS
        SET USERS.AGE=#{item.age},
        USERS.NAME=#{item.name}
        WHERE USERS.ID=#{item.id}
    </foreach>
    </update>
</mapper>